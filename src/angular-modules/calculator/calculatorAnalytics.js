/**
 * @description on bootstrap, calculator will start listening to all calculator methods and
 *  send them to google analytics
 */
angular.module('calculator').run(function(){
    // not important
});